--[[
    SOU HUB - TOUCHLINE EDITION v2
    Reach + Ball Hitbox + Goal Aimbot + Auto Dive
    Ball ESP + Hitbox ESP + Ball Hitbox ESP + Keybind Sistemi
    ESC Fix + Hizli Leave Fix
]]

print("[SOU HUB] Touchline Edition v2 yukleniyor...")

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local GuiService = game:GetService("GuiService")
local Lighting = game:GetService("Lighting")
local Stats = game:GetService("Stats")
local TweenService = game:GetService("TweenService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()

local function getGuiParent()
    if gethui then return gethui() end
    if syn and syn.protect_gui then
        local sg = Instance.new("ScreenGui")
        syn.protect_gui(sg)
        sg.Parent = game:GetService("CoreGui")
        return sg
    end
    local ok = pcall(function()
        local t = Instance.new("ScreenGui"); t.Parent = game:GetService("CoreGui"); t:Destroy()
    end)
    if ok then return game:GetService("CoreGui") end
    return LocalPlayer:WaitForChild("PlayerGui")
end

-- =============================================
-- THEME
-- =============================================
local Theme = {
    Bg = Color3.fromRGB(10, 18, 32),
    Panel = Color3.fromRGB(16, 28, 46),
    Header = Color3.fromRGB(14, 24, 40),
    Button = Color3.fromRGB(26, 42, 66),
    Slider = Color3.fromRGB(30, 48, 72),
    Accent = Color3.fromRGB(140, 200, 255),
    Accent2 = Color3.fromRGB(232, 244, 255),
    Text = Color3.fromRGB(220, 235, 250),
    SubText = Color3.fromRGB(140, 165, 195),
    Border = Color3.fromRGB(45, 70, 105),
    ToggleOn = Color3.fromRGB(120, 200, 255),
    ToggleOff = Color3.fromRGB(50, 70, 100),
}

local OriginalLighting = {
    Ambient = Lighting.Ambient, OutdoorAmbient = Lighting.OutdoorAmbient,
    Brightness = Lighting.Brightness, FogEnd = Lighting.FogEnd,
}

-- =============================================
-- SETTINGS
-- =============================================
local Settings = {
    -- Ball Control
    Reach = false, ReachValue = 30,
    BallHitbox = false, BallSize = 2.0,
    GoalAimbot = false, GoalAimStrength = 0.5,
    AutoDive = false,
    Prediction = false,
    Fullbright = false,
    
    -- Ball ESP
    BallESP = false,
    BallESPBox = true,
    BallESPName = true,
    BallESPDistance = true,
    BallESPTracer = false,
    BallESPColor = Color3.fromRGB(255, 220, 60),
    
    -- Hitbox ESP (görsel)
    HitboxESP = false,
    HitboxESPTransparency = 0.7,
    HitboxESPColor = Color3.fromRGB(120, 220, 160),
    
    -- Ball Hitbox ESP (YENİ)
    BallHitboxESP = false,
    BallHitboxESPTransparency = 0.5,
    BallHitboxESPColor = Color3.fromRGB(120, 255, 140),
    
    Watermark = true,
    GuiTransparency = 250,
}

-- =============================================
-- KEYBIND SYSTEM
-- =============================================
local activeKeybindBtn = nil
local keybindCallbacks = {}
_G.SOUHUB_KeybindAssigners = _G.SOUHUB_KeybindAssigners or {}

-- =============================================
-- CLEANUP
-- =============================================
for _, loc in ipairs({game:GetService("CoreGui"), LocalPlayer:FindFirstChild("PlayerGui")}) do
    pcall(function()
        local o = loc:FindFirstChild("SOUHUB_Touchline"); if o then o:Destroy() end
        local o2 = loc:FindFirstChild("SOUHUB_Snow"); if o2 then o2:Destroy() end
    end)
end
pcall(function()
    if gethui then
        local o = gethui():FindFirstChild("SOUHUB_Touchline"); if o then o:Destroy() end
        local o2 = gethui():FindFirstChild("SOUHUB_Snow"); if o2 then o2:Destroy() end
    end
end)

local function tween(obj, time, props, style, dir)
    local info = TweenInfo.new(time or 0.2, style or Enum.EasingStyle.Quint, dir or Enum.EasingDirection.Out)
    local t = TweenService:Create(obj, info, props)
    t:Play()
    return t
end

-- =============================================
-- GUI
-- =============================================
local guiParent = getGuiParent()
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SOUHUB_Touchline"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.DisplayOrder = 999
ScreenGui.IgnoreGuiInset = true
if guiParent:IsA("ScreenGui") then
    ScreenGui = guiParent; ScreenGui.Name = "SOUHUB_Touchline"; ScreenGui.ResetOnSpawn = false; ScreenGui.DisplayOrder = 999
else ScreenGui.Parent = guiParent end

-- =============================================
-- KAR YAGISI
-- =============================================
local snowGui = Instance.new("ScreenGui")
snowGui.Name = "SOUHUB_Snow"
snowGui.ResetOnSpawn = false
snowGui.DisplayOrder = 1
snowGui.IgnoreGuiInset = true
snowGui.Parent = guiParent

local snowContainer = Instance.new("Frame")
snowContainer.Size = UDim2.new(1, 0, 1, 0)
snowContainer.BackgroundTransparency = 1
snowContainer.Parent = snowGui

local function createSnowflake()
    local sf = Instance.new("TextLabel")
    sf.Size = UDim2.new(0, math.random(8, 18), 0, math.random(8, 18))
    sf.Position = UDim2.new(math.random(), 0, -0.05, 0)
    sf.BackgroundTransparency = 1
    sf.Text = "❄"
    sf.TextColor3 = Color3.fromRGB(255, 255, 255)
    sf.TextSize = math.random(8, 18)
    sf.TextTransparency = math.random(3, 7) / 10
    sf.Font = Enum.Font.GothamBold
    sf.Rotation = math.random(0, 360)
    sf.Parent = snowContainer
    local dur = math.random(6, 14)
    local drift = math.random(-20, 20) / 100
    tween(sf, dur, {Position = UDim2.new(sf.Position.X.Scale + drift, 0, 1.1, 0), Rotation = sf.Rotation + math.random(-180, 180)}, Enum.EasingStyle.Linear)
    task.delay(dur, function() if sf and sf.Parent then sf:Destroy() end end)
end

task.spawn(function()
    while snowContainer.Parent do
        createSnowflake()
        task.wait(math.random(5, 15) / 100)
    end
end)

-- =============================================
-- TOAST
-- =============================================
local toastContainer = Instance.new("Frame")
toastContainer.Size = UDim2.new(0, 320, 1, -20)
toastContainer.Position = UDim2.new(1, -340, 0, 10)
toastContainer.BackgroundTransparency = 1
toastContainer.ZIndex = 1000
toastContainer.Parent = ScreenGui
local toastLayout = Instance.new("UIListLayout", toastContainer)
toastLayout.SortOrder = Enum.SortOrder.LayoutOrder
toastLayout.Padding = UDim.new(0, 8)
toastLayout.VerticalAlignment = Enum.VerticalAlignment.Top

local function showToast(title, message, toastType)
    toastType = toastType or "info"
    local colors = {info = Theme.Accent, success = Color3.fromRGB(80, 200, 130), warning = Color3.fromRGB(230, 180, 60), error = Color3.fromRGB(220, 70, 80)}
    local toast = Instance.new("Frame")
    toast.Size = UDim2.new(0, 0, 0, 52)
    toast.Position = UDim2.new(1, 20, 0, 0)
    toast.BackgroundColor3 = Theme.Panel
    toast.BackgroundTransparency = 0.05
    toast.BorderSizePixel = 0
    toast.ZIndex = 1001
    toast.Parent = toastContainer
    Instance.new("UICorner", toast).CornerRadius = UDim.new(0, 6)
    local stroke = Instance.new("UIStroke", toast)
    stroke.Color = colors[toastType]; stroke.Thickness = 1.5; stroke.Transparency = 0.3
    local bar = Instance.new("Frame")
    bar.Size = UDim2.new(0, 3, 1, 0); bar.BackgroundColor3 = colors[toastType]
    bar.BorderSizePixel = 0; bar.ZIndex = 1002; bar.Parent = toast
    local titleLbl = Instance.new("TextLabel")
    titleLbl.Size = UDim2.new(1, -24, 0, 18); titleLbl.Position = UDim2.new(0, 16, 0, 10)
    titleLbl.BackgroundTransparency = 1; titleLbl.Text = title
    titleLbl.TextColor3 = Theme.Text; titleLbl.TextSize = 12
    titleLbl.Font = Enum.Font.GothamBold; titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.ZIndex = 1002; titleLbl.Parent = toast
    local msgLbl = Instance.new("TextLabel")
    msgLbl.Size = UDim2.new(1, -24, 0, 16); msgLbl.Position = UDim2.new(0, 16, 0, 28)
    msgLbl.BackgroundTransparency = 1; msgLbl.Text = message
    msgLbl.TextColor3 = Theme.SubText; msgLbl.TextSize = 10
    msgLbl.Font = Enum.Font.Gotham; msgLbl.TextXAlignment = Enum.TextXAlignment.Left
    msgLbl.ZIndex = 1002; msgLbl.Parent = toast
    tween(toast, 0.4, {Size = UDim2.new(0, 320, 0, 52), Position = UDim2.new(0, 0, 0, 0)}, Enum.EasingStyle.Quint)
    task.delay(3, function()
        tween(toast, 0.3, {Position = UDim2.new(1, 20, 0, 0)})
        tween(toast, 0.3, {BackgroundTransparency = 1})
        toast:Destroy()
    end)
end

-- =============================================
-- DRAGGABLE
-- =============================================
local function makeDraggable(frame, handle)
    local dragging, dragInput, dragStart, startPos
    handle = handle or frame
    handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true; dragStart = input.Position; startPos = frame.Position
            input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
        end
    end)
    handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local d = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + d.X, startPos.Y.Scale, startPos.Y.Offset + d.Y)
        end
    end)
end

-- =============================================
-- MAIN FRAME
-- =============================================
local InitialTransparency = 1 - (Settings.GuiTransparency / 500)

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 620, 0, 480)
MainFrame.Position = UDim2.new(0.5, -310, 0.5, -240)
MainFrame.BackgroundColor3 = Theme.Bg
MainFrame.BackgroundTransparency = InitialTransparency
MainFrame.BorderSizePixel = 0
MainFrame.Active = false
MainFrame.Visible = true
MainFrame.Parent = ScreenGui

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

local mainStroke = Instance.new("UIStroke", MainFrame)
mainStroke.Color = Theme.Border
mainStroke.Thickness = 1.5
mainStroke.Transparency = InitialTransparency + 0.2

local topAccent = Instance.new("Frame")
topAccent.Size = UDim2.new(0, 120, 0, 1)
topAccent.Position = UDim2.new(0, 24, 0, 1)
topAccent.BackgroundColor3 = Theme.Accent
topAccent.BorderSizePixel = 0
topAccent.ZIndex = 5
topAccent.Parent = MainFrame

local DragHandle = Instance.new("TextButton")
DragHandle.Size = UDim2.new(1, 0, 0, 50); DragHandle.BackgroundTransparency = 1
DragHandle.Text = ""; DragHandle.AutoButtonColor = false; DragHandle.ZIndex = 10; DragHandle.Parent = MainFrame
makeDraggable(MainFrame, DragHandle)

local mainGradient = Instance.new("UIGradient", MainFrame)
mainGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(Theme.Bg.R*255 + 6, Theme.Bg.G*255 + 6, Theme.Bg.B*255 + 6)),
    ColorSequenceKeypoint.new(1, Theme.Bg),
})
mainGradient.Rotation = 135

local tl = Instance.new("TextLabel")
tl.Size = UDim2.new(1, 0, 0, 22); tl.Position = UDim2.new(0, 24, 0, 14)
tl.BackgroundTransparency = 1; tl.Text = "❄ SOU HUB"; tl.TextColor3 = Theme.Text
tl.TextSize = 18; tl.Font = Enum.Font.GothamBold
tl.TextXAlignment = Enum.TextXAlignment.Left; tl.ZIndex = 5; tl.Parent = MainFrame

local cl = Instance.new("TextLabel")
cl.Size = UDim2.new(1, 0, 0, 14); cl.Position = UDim2.new(0, 24, 0, 34)
cl.BackgroundTransparency = 1; cl.Text = "TOUCHLINE EDITION v2"; cl.TextColor3 = Theme.SubText
cl.TextSize = 9; cl.Font = Enum.Font.GothamSemibold
cl.TextXAlignment = Enum.TextXAlignment.Left; cl.ZIndex = 5; cl.Parent = MainFrame

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 28, 0, 28)
closeBtn.Position = UDim2.new(1, -38, 0, 12)
closeBtn.BackgroundColor3 = Theme.Button
closeBtn.BackgroundTransparency = 0.3
closeBtn.BorderSizePixel = 0; closeBtn.Text = "×"
closeBtn.TextColor3 = Theme.SubText; closeBtn.TextSize = 20
closeBtn.Font = Enum.Font.Gotham; closeBtn.AutoButtonColor = false
closeBtn.ZIndex = 11; closeBtn.Parent = MainFrame
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 4)
closeBtn.MouseEnter:Connect(function()
    tween(closeBtn, 0.15, {BackgroundColor3 = Color3.fromRGB(180, 50, 50), BackgroundTransparency = 0, TextColor3 = Color3.fromRGB(255,255,255)})
end)
closeBtn.MouseLeave:Connect(function()
    tween(closeBtn, 0.15, {BackgroundColor3 = Theme.Button, BackgroundTransparency = 0.3, TextColor3 = Theme.SubText})
end)
closeBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

-- =============================================
-- UI BUILDERS
-- =============================================
local function addToggle(parent, name, default, callback, order, withKeybind)
    local row = Instance.new("Frame")
    row.Size = UDim2.new(1, -16, 0, 36)
    row.BackgroundTransparency = 1
    row.LayoutOrder = order or 0
    row.ZIndex = 3
    row.Parent = parent
    
    local toggleWidth = withKeybind and UDim2.new(1, -76, 1, 0) or UDim2.new(1, 0, 1, 0)
    
    local btn = Instance.new("TextButton")
    btn.Size = toggleWidth
    btn.BackgroundColor3 = Theme.Button
    btn.BackgroundTransparency = 0.3
    btn.BorderSizePixel = 0
    btn.Text = ""
    btn.AutoButtonColor = false
    btn.ZIndex = 3
    btn.Parent = row
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    
    local nameLbl = Instance.new("TextLabel")
    nameLbl.Size = UDim2.new(0.7, 0, 1, 0)
    nameLbl.Position = UDim2.new(0, 14, 0, 0)
    nameLbl.BackgroundTransparency = 1
    nameLbl.Text = name
    nameLbl.TextColor3 = Theme.Text
    nameLbl.TextSize = 12
    nameLbl.Font = Enum.Font.GothamSemibold
    nameLbl.TextXAlignment = Enum.TextXAlignment.Left
    nameLbl.ZIndex = 4
    nameLbl.Parent = btn
    
    local stateLbl = Instance.new("TextLabel")
    stateLbl.Size = UDim2.new(0, 50, 1, 0)
    stateLbl.Position = UDim2.new(1, -60, 0, 0)
    stateLbl.BackgroundTransparency = 1
    stateLbl.Text = default and "ON" or "OFF"
    stateLbl.TextColor3 = default and Theme.Accent or Theme.SubText
    stateLbl.TextSize = 11
    stateLbl.Font = Enum.Font.GothamBold
    stateLbl.TextXAlignment = Enum.TextXAlignment.Right
    stateLbl.ZIndex = 4
    stateLbl.Parent = btn
    
    local state = default
    local function doToggle()
        state = not state
        tween(btn, 0.2, {BackgroundColor3 = state and Color3.fromRGB(40, 70, 110) or Theme.Button})
        tween(stateLbl, 0.2, {TextColor3 = state and Theme.Accent or Theme.SubText})
        stateLbl.Text = state and "ON" or "OFF"
        if callback then callback(state) end
    end
    btn.MouseButton1Click:Connect(doToggle)
    
    -- Keybind butonu
    if withKeybind then
        local kbBtn = Instance.new("TextButton")
        kbBtn.Size = UDim2.new(0, 68, 1, 0)
        kbBtn.Position = UDim2.new(1, -68, 0, 0)
        kbBtn.BackgroundColor3 = Theme.Button
        kbBtn.BackgroundTransparency = 0.5
        kbBtn.BorderSizePixel = 0
        kbBtn.Text = "[ - ]"
        kbBtn.TextColor3 = Theme.SubText
        kbBtn.TextSize = 10
        kbBtn.Font = Enum.Font.GothamBold
        kbBtn.AutoButtonColor = false
        kbBtn.ZIndex = 4
        kbBtn.Parent = row
        Instance.new("UICorner", kbBtn).CornerRadius = UDim.new(0, 4)
        local kbStroke = Instance.new("UIStroke", kbBtn)
        kbStroke.Color = Theme.Border
        kbStroke.Thickness = 1
        
        local assignedKey = nil
        
        kbBtn.MouseButton1Click:Connect(function()
            if assignedKey then
                keybindCallbacks[assignedKey] = nil
                assignedKey = nil
            end
            
            if activeKeybindBtn == kbBtn then
                activeKeybindBtn = nil
                kbBtn.Text = "[ - ]"
                kbBtn.TextColor3 = Theme.SubText
                tween(kbBtn, 0.2, {BackgroundTransparency = 0.5})
                return
            end
            
            if activeKeybindBtn then
                activeKeybindBtn.Text = "[ - ]"
                activeKeybindBtn.TextColor3 = Theme.SubText
                tween(activeKeybindBtn, 0.2, {BackgroundTransparency = 0.5})
            end
            
            activeKeybindBtn = kbBtn
            kbBtn.Text = "[...]"
            kbBtn.TextColor3 = Color3.fromRGB(255, 200, 60)
            tween(kbBtn, 0.2, {BackgroundTransparency = 0.2})
        end)
        
        _G.SOUHUB_KeybindAssigners[kbBtn] = function(keyCode)
            assignedKey = keyCode
            kbBtn.Text = "[" .. keyCode.Name .. "]"
            kbBtn.TextColor3 = Color3.fromRGB(120, 220, 160)
            tween(kbBtn, 0.15, {BackgroundTransparency = 0.3})
            keybindCallbacks[keyCode] = doToggle
            activeKeybindBtn = nil
        end
    end
    
    return function() return state end
end

local function addSlider(parent, name, min, max, default, callback, order)
    local container = Instance.new("Frame")
    container.Size = UDim2.new(1, -16, 0, 50)
    container.BackgroundTransparency = 1
    container.LayoutOrder = order or 0
    container.ZIndex = 3
    container.Parent = parent
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, 0, 0, 18)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = Theme.Text
    label.TextSize = 12
    label.Font = Enum.Font.GothamSemibold
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.ZIndex = 3
    label.Parent = container
    
    local valueLbl = Instance.new("TextLabel")
    valueLbl.Size = UDim2.new(0.3, 0, 0, 18)
    valueLbl.Position = UDim2.new(0.7, 0, 0, 0)
    valueLbl.BackgroundTransparency = 1
    valueLbl.Text = string.format("%.2f", default)
    valueLbl.TextColor3 = Theme.Accent
    valueLbl.TextSize = 11
    valueLbl.Font = Enum.Font.GothamBold
    valueLbl.TextXAlignment = Enum.TextXAlignment.Right
    valueLbl.ZIndex = 3
    valueLbl.Parent = container
    
    local bg = Instance.new("TextButton")
    bg.Size = UDim2.new(1, 0, 0, 10)
    bg.Position = UDim2.new(0, 0, 0, 28)
    bg.BackgroundColor3 = Theme.Button
    bg.BackgroundTransparency = 0.3
    bg.BorderSizePixel = 0
    bg.Text = ""
    bg.AutoButtonColor = false
    bg.ZIndex = 3
    bg.Parent = container
    Instance.new("UICorner", bg).CornerRadius = UDim.new(1, 0)
    
    local fill = Instance.new("Frame")
    fill.Size = UDim2.new((default-min)/(max-min), 0, 1, 0)
    fill.BackgroundColor3 = Theme.Accent
    fill.BorderSizePixel = 0
    fill.ZIndex = 3
    fill.Parent = bg
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)
    
    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 14, 0, 14)
    knob.AnchorPoint = Vector2.new(0.5, 0.5)
    knob.Position = UDim2.new((default-min)/(max-min), 0, 0.5, 0)
    knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    knob.BorderSizePixel = 0
    knob.ZIndex = 4
    knob.Parent = bg
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)
    
    local value = default
    local sliding = false
    local function update(px)
        local ax, as = bg.AbsolutePosition.X, bg.AbsoluteSize.X
        if as == 0 then return end
        local p = math.clamp((px - ax) / as, 0, 1)
        value = min + (max - min) * p
        fill.Size = UDim2.new(p, 0, 1, 0)
        knob.Position = UDim2.new(p, 0, 0.5, 0)
        valueLbl.Text = string.format("%.2f", value)
        if callback then callback(value) end
    end
    bg.MouseButton1Down:Connect(function(x) sliding = true; update(x) end)
    UserInputService.InputChanged:Connect(function(input)
        if sliding and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then update(input.Position.X) end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then sliding = false end
    end)
    return function() return value end
end

local function addLabel(page, text, order)
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, -16, 0, 22)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = Theme.Accent
    lbl.TextSize = 10
    lbl.Font = Enum.Font.GothamBold
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.LayoutOrder = order or 0
    lbl.ZIndex = 3
    lbl.Parent = page
end

local function addCycleButton(page, name, options, default, callback, order)
    local idx = 1
    for i,v in ipairs(options) do if v == default then idx = i; break end end
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1,-16,0,32)
    btn.BackgroundColor3 = Theme.Button
    btn.BackgroundTransparency = 0.3
    btn.BorderSizePixel = 0
    btn.Text = ""
    btn.AutoButtonColor = false
    btn.LayoutOrder = order or 0
    btn.ZIndex = 3
    btn.Parent = page
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0,5)
    local s = Instance.new("UIStroke", btn); s.Color = Theme.Border; s.Thickness = 1
    
    local nameLbl = Instance.new("TextLabel")
    nameLbl.Size = UDim2.new(0.5, 0, 1, 0)
    nameLbl.Position = UDim2.new(0, 14, 0, 0)
    nameLbl.BackgroundTransparency = 1
    nameLbl.Text = name
    nameLbl.TextColor3 = Theme.Text
    nameLbl.TextSize = 11
    nameLbl.Font = Enum.Font.GothamSemibold
    nameLbl.TextXAlignment = Enum.TextXAlignment.Left
    nameLbl.ZIndex = 4
    nameLbl.Parent = btn
    
    local valueLbl = Instance.new("TextLabel")
    valueLbl.Size = UDim2.new(0.5, -14, 1, 0)
    valueLbl.Position = UDim2.new(0.5, 0, 0, 0)
    valueLbl.BackgroundTransparency = 1
    valueLbl.Text = options[idx]
    valueLbl.TextColor3 = Theme.Accent
    valueLbl.TextSize = 11
    valueLbl.Font = Enum.Font.GothamBold
    valueLbl.TextXAlignment = Enum.TextXAlignment.Right
    valueLbl.ZIndex = 4
    valueLbl.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        idx = idx % #options + 1
        valueLbl.Text = options[idx]
        if callback then callback(options[idx]) end
    end)
    return function() return options[idx] end
end

-- =============================================
-- CONTENT AREA
-- =============================================
local ContentArea = Instance.new("Frame")
ContentArea.Size = UDim2.new(1, -20, 1, -70)
ContentArea.Position = UDim2.new(0, 10, 0, 60)
ContentArea.BackgroundTransparency = 1
ContentArea.ZIndex = 3
ContentArea.Parent = MainFrame

local mainPage = Instance.new("ScrollingFrame")
mainPage.Size = UDim2.new(1, 0, 1, 0)
mainPage.BackgroundTransparency = 1
mainPage.BorderSizePixel = 0
mainPage.ScrollBarThickness = 3
mainPage.ScrollBarImageColor3 = Theme.Accent
mainPage.CanvasSize = UDim2.new(0, 0, 0, 0)
mainPage.AutomaticCanvasSize = Enum.AutomaticSize.Y
mainPage.ZIndex = 2
mainPage.Active = true
mainPage.Parent = ContentArea

local layout = Instance.new("UIListLayout", mainPage)
layout.SortOrder = Enum.SortOrder.LayoutOrder
layout.Padding = UDim.new(0, 6)
local pad = Instance.new("UIPadding", mainPage)
pad.PaddingLeft = UDim.new(0, 4)
pad.PaddingRight = UDim.new(0, 4)
pad.PaddingTop = UDim.new(0, 4)

addLabel(mainPage, "⚽ BALL CONTROL", 1)
local getReach = addToggle(mainPage, "Reach", false, function(v) Settings.Reach = v end, 2, true)
local getReachValue = addSlider(mainPage, "Reach Distance", 5, 100, 30, function(v) Settings.ReachValue = v end, 3)

addLabel(mainPage, "⚽ BALL HITBOX", 4)
local getBallHitbox = addToggle(mainPage, "Ball Hitbox", false, function(v) Settings.BallHitbox = v end, 5, true)
local getBallSize = addSlider(mainPage, "Ball Size", 1.0, 5.0, 2.0, function(v) Settings.BallSize = v end, 6)

addLabel(mainPage, "🎯 BALL ESP", 7)
local getBallESP = addToggle(mainPage, "Ball ESP", false, function(v) Settings.BallESP = v end, 8, true)
local getBallESPBox = addToggle(mainPage, "  Box", true, function(v) Settings.BallESPBox = v end, 9, false)
local getBallESPName = addToggle(mainPage, "  Name", true, function(v) Settings.BallESPName = v end, 10, false)
local getBallESPDistance = addToggle(mainPage, "  Distance", true, function(v) Settings.BallESPDistance = v end, 11, false)
local getBallESPTracer = addToggle(mainPage, "  Tracer", false, function(v) Settings.BallESPTracer = v end, 12, false)
local getBallESPColor = addCycleButton(mainPage, "ESP Color", {"Yellow","Red","Cyan","Green","Purple","White"}, "Yellow", function(v)
    local colors = {Yellow=Color3.fromRGB(255,220,60), Red=Color3.fromRGB(255,80,80), Cyan=Color3.fromRGB(100,220,255), Green=Color3.fromRGB(100,255,140), Purple=Color3.fromRGB(200,120,255), White=Color3.fromRGB(255,255,255)}
    Settings.BallESPColor = colors[v] or Color3.fromRGB(255,220,60)
end, 13)

-- =============================================
-- BALL HITBOX ESP (YENİ)
-- =============================================
addLabel(mainPage, "📦 BALL HITBOX ESP", 14)
local getBallHitboxESP = addToggle(mainPage, "Ball Hitbox ESP", false, function(v) Settings.BallHitboxESP = v end, 15, true)
local getBallHitboxTransparency = addSlider(mainPage, "Ball Hitbox Transparency", 0.1, 0.9, 0.5, function(v) Settings.BallHitboxESPTransparency = v end, 16)
local getBallHitboxColor = addCycleButton(mainPage, "Ball Hitbox Color", {"Green","Red","Cyan","Yellow","Purple","White"}, "Green", function(v)
    local colors = {Green=Color3.fromRGB(120,255,140), Red=Color3.fromRGB(255,80,80), Cyan=Color3.fromRGB(100,220,255), Yellow=Color3.fromRGB(255,220,60), Purple=Color3.fromRGB(200,120,255), White=Color3.fromRGB(255,255,255)}
    Settings.BallHitboxESPColor = colors[v] or Color3.fromRGB(120,255,140)
end, 17)

addLabel(mainPage, "📦 HITBOX ESP", 18)
local getHitboxESP = addToggle(mainPage, "Hitbox ESP", false, function(v) Settings.HitboxESP = v end, 19, true)
local getHitboxTransparency = addSlider(mainPage, "Hitbox Transparency", 0.1, 0.9, 0.7, function(v) Settings.HitboxESPTransparency = v end, 20)

addLabel(mainPage, "🥅 GOAL AIMBOT", 21)
local getGoalAimbot = addToggle(mainPage, "Goal Aimbot", false, function(v) Settings.GoalAimbot = v end, 22, true)
local getGoalStrength = addSlider(mainPage, "Aim Strength", 0.1, 1.0, 0.5, function(v) Settings.GoalAimStrength = v end, 23)

addLabel(mainPage, "🧤 GOALKEEPER", 24)
local getAutoDive = addToggle(mainPage, "Auto Dive", false, function(v) Settings.AutoDive = v end, 25, true)

addLabel(mainPage, "🌍 WORLD", 26)
local getFullbright = addToggle(mainPage, "Fullbright", false, function(v) Settings.Fullbright = v end, 27, true)

-- =============================================
-- KEYBIND LISTENER
-- =============================================
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if activeKeybindBtn then
        local assigner = _G.SOUHUB_KeybindAssigners[activeKeybindBtn]
        if assigner then
            assigner(input.KeyCode)
        end
        return
    end
    if keybindCallbacks[input.KeyCode] then
        keybindCallbacks[input.KeyCode]()
    end
end)

-- =============================================
-- TOP/KALE BULMA
-- =============================================
local function getBall()
    local ball = workspace:FindFirstChild("Ball")
    if ball and ball:IsA("BasePart") then return ball end
    ball = workspace:FindFirstChild("ball")
    if ball and ball:IsA("BasePart") then return ball end
    for _, obj in ipairs(workspace:GetChildren()) do
        if obj:IsA("BasePart") and (obj.Name:lower():find("ball") or obj.Name:lower():find("top")) then
            return obj
        end
    end
    return nil
end

local function getGoal()
    for _, obj in ipairs(workspace:GetChildren()) do
        if obj.Name:lower():find("goal") or obj.Name:lower():find("kale") then
            if obj:IsA("BasePart") then return obj end
            local part = obj:FindFirstChildWhichIsA("BasePart")
            if part then return part end
        end
    end
    return nil
end

-- =============================================
-- BALL HITBOX ESP RENDER (YENİ)
-- =============================================
local ballHitboxESPStorage = {}

local function getOrCreateBallHitboxESP()
    local ball = getBall()
    if not ball then return nil end
    
    local key = "ball_hitbox_esp"
    if ballHitboxESPStorage[key] and ballHitboxESPStorage[key].Parent then
        return ballHitboxESPStorage[key]
    end
    
    local box = Instance.new("BoxHandleAdornment")
    box.Name = "SOUHUB_BallHitboxESP"
    box.Adornee = ball
    box.AlwaysOnTop = true
    box.ZIndex = 5
    box.Size = ball.Size + Vector3.new(0.2, 0.2, 0.2)
    box.Color3 = Settings.BallHitboxESPColor
    box.Transparency = Settings.BallHitboxESPTransparency
    box.Parent = game:GetService("CoreGui")
    
    ballHitboxESPStorage[key] = box
    return box
end

-- =============================================
-- REACH
-- =============================================
RunService.Heartbeat:Connect(function()
    if Settings.Reach then
        local char = LocalPlayer.Character
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            local ball = getBall()
            if hrp and ball then
                local dist = (hrp.Position - ball.Position).Magnitude
                if dist <= Settings.ReachValue then
                    -- Reach aktif - topa dokunma mesafesi artırıldı
                end
            end
        end
    end
end)

-- =============================================
-- BALL HITBOX
-- =============================================
local originalBallSize = nil

RunService.Heartbeat:Connect(function()
    local ball = getBall()
    if not ball then return end
    if Settings.BallHitbox then
        if not originalBallSize then originalBallSize = ball.Size end
        local s = Settings.BallSize
        ball.Size = Vector3.new(s, s, s)
        ball.CanCollide = true
    else
        if originalBallSize then
            ball.Size = originalBallSize
            originalBallSize = nil
        end
    end
end)

-- =============================================
-- BALL ESP RENDER
-- =============================================
local ballESPStorage = {}

local function createBallESP()
    local ball = getBall()
    if not ball then return end
    
    local billboard = Instance.new("BillboardGui")
    billboard.Name = "SOUHUB_BallESP"
    billboard.Size = UDim2.new(0, 120, 0, 50)
    billboard.StudsOffset = Vector3.new(0, 2, 0)
    billboard.AlwaysOnTop = true
    billboard.Adornee = ball
    billboard.Parent = game:GetService("CoreGui")
    
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundTransparency = 1
    frame.Parent = billboard
    
    local box = Instance.new("Frame")
    box.Name = "Box"
    box.Size = UDim2.new(1, 0, 1, 0)
    box.BackgroundTransparency = 1
    box.BorderSizePixel = 2
    box.BorderColor3 = Settings.BallESPColor
    box.Parent = frame
    
    local nameLbl = Instance.new("TextLabel")
    nameLbl.Name = "NameLbl"
    nameLbl.Size = UDim2.new(1, 0, 0, 16)
    nameLbl.Position = UDim2.new(0, 0, 0, -18)
    nameLbl.BackgroundTransparency = 1
    nameLbl.Text = "⚽ BALL"
    nameLbl.TextColor3 = Settings.BallESPColor
    nameLbl.TextSize = 12
    nameLbl.Font = Enum.Font.GothamBold
    nameLbl.TextStrokeTransparency = 0.3
    nameLbl.Parent = frame
    
    local distLbl = Instance.new("TextLabel")
    distLbl.Name = "DistLbl"
    distLbl.Size = UDim2.new(1, 0, 0, 14)
    distLbl.Position = UDim2.new(0, 0, 1, 2)
    distLbl.BackgroundTransparency = 1
    distLbl.Text = "0m"
    distLbl.TextColor3 = Settings.BallESPColor
    distLbl.TextSize = 11
    distLbl.Font = Enum.Font.GothamBold
    distLbl.TextStrokeTransparency = 0.3
    distLbl.Parent = frame
    
    ballESPStorage[ball] = {gui = billboard, box = box, nameLbl = nameLbl, distLbl = distLbl}
end

local function removeBallESP(ball)
    if ballESPStorage[ball] then
        pcall(function() ballESPStorage[ball].gui:Destroy() end)
        ballESPStorage[ball] = nil
    end
end

-- =============================================
-- HITBOX ESP RENDER
-- =============================================
local hitboxESPStorage = {}

local function createHitboxESP(part)
    local box = Instance.new("BoxHandleAdornment")
    box.Name = "SOUHUB_HitboxESP"
    box.Adornee = part
    box.AlwaysOnTop = true
    box.ZIndex = 5
    box.Size = part.Size
    box.Color3 = Settings.HitboxESPColor
    box.Transparency = Settings.HitboxESPTransparency
    box.Parent = game:GetService("CoreGui")
    hitboxESPStorage[part] = box
end

-- =============================================
-- MAIN RENDER LOOP
-- =============================================
RunService.RenderStepped:Connect(function()
    -- Ball ESP
    local ball = getBall()
    if Settings.BallESP and ball then
        if not ballESPStorage[ball] then createBallESP() end
        local data = ballESPStorage[ball]
        if data then
            data.box.BorderColor3 = Settings.BallESPColor
            data.nameLbl.TextColor3 = Settings.BallESPColor
            data.distLbl.TextColor3 = Settings.BallESPColor
            data.box.Visible = Settings.BallESPBox
            data.nameLbl.Visible = Settings.BallESPName
            data.distLbl.Visible = Settings.BallESPDistance
            
            local char = LocalPlayer.Character
            if char and char:FindFirstChild("HumanoidRootPart") then
                local dist = (char.HumanoidRootPart.Position - ball.Position).Magnitude
                data.distLbl.Text = string.format("%.0fm", dist)
            end
        end
    else
        for b, data in pairs(ballESPStorage) do
            pcall(function() data.gui:Destroy() end)
        end
        ballESPStorage = {}
    end
    
    -- Ball Hitbox ESP (YENİ)
    if Settings.BallHitboxESP and ball then
        local box = getOrCreateBallHitboxESP()
        if box then
            box.Adornee = ball
            box.Size = ball.Size + Vector3.new(0.2, 0.2, 0.2)
            box.Color3 = Settings.BallHitboxESPColor
            box.Transparency = Settings.BallHitboxESPTransparency
            box.Visible = true
        end
    else
        for _, box in pairs(ballHitboxESPStorage) do
            if box and box.Parent then
                box.Visible = false
            end
        end
    end
    
    -- Hitbox ESP (karakterler)
    if Settings.HitboxESP then
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= LocalPlayer and plr.Character then
                local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    if not hitboxESPStorage[hrp] then
                        createHitboxESP(hrp)
                    end
                    local box = hitboxESPStorage[hrp]
                    if box then
                        box.Size = hrp.Size
                        box.Color3 = Settings.HitboxESPColor
                        box.Transparency = Settings.HitboxESPTransparency
                    end
                end
            end
        end
    else
        for part, box in pairs(hitboxESPStorage) do
            if box and box.Parent then box:Destroy() end
            hitboxESPStorage[part] = nil
        end
    end
end)

-- =============================================
-- GOAL AIMBOT
-- =============================================
RunService.RenderStepped:Connect(function()
    if not Settings.GoalAimbot then return end
    local ball = getBall()
    local goal = getGoal()
    if not ball or not goal then return end
    
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    local dist = (hrp.Position - ball.Position).Magnitude
    if dist < 30 then
        local targetCF = CFrame.new(ball.Position, goal.Position)
        local currentCF = ball.CFrame
        local newCF = currentCF:Lerp(targetCF, Settings.GoalAimStrength * 0.1)
        ball.CFrame = newCF
    end
end)

-- =============================================
-- AUTO DIVE
-- =============================================
RunService.Heartbeat:Connect(function()
    if not Settings.AutoDive then return end
    local ball = getBall()
    if not ball then return end
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    local dist = (hrp.Position - ball.Position).Magnitude
    if dist < 25 then
        local goal = getGoal()
        if goal then
            local dir = (goal.Position - hrp.Position).Unit
            hrp.CFrame = CFrame.new(hrp.Position, hrp.Position + dir)
        end
    end
end)

-- =============================================
-- FULLBRIGHT
-- =============================================
RunService.Heartbeat:Connect(function()
    if Settings.Fullbright then
        Lighting.Ambient = Color3.fromRGB(255, 255, 255)
        Lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
        Lighting.Brightness = 2
        Lighting.FogEnd = 100000
    else
        Lighting.Ambient = OriginalLighting.Ambient
        Lighting.OutdoorAmbient = OriginalLighting.OutdoorAmbient
        Lighting.Brightness = OriginalLighting.Brightness
        Lighting.FogEnd = OriginalLighting.FogEnd
    end
end)

-- =============================================
-- ESC FIX
-- =============================================
pcall(function()
    GuiService:SetMenuIsOpen(false)
    GuiService.MenuOpened:Connect(function()
        GuiService:SetMenuIsOpen(false)
    end)
end)

-- =============================================
-- INIT
-- =============================================
showToast("SOU HUB", "Touchline Edition v2 yüklendi!", "success")
task.wait(0.5)
showToast("Ball Hitbox ESP", "Yeni özellik eklendi!", "info")

print("[SOU HUB] Touchline Edition v2 hazır!")
